#form
  = form_for(@category) do |f|
    - if @category.errors.any?
      #error_explanation
        %h2
          = pluralize(@category.errors.count, "erro")
          esta categoria não pode ser salva:
        %ul
          - @category.errors.full_messages.each do |msg|
            %li= msg
    .field
      %b Nome da categoria*
      %br/
      = f.text_field :name
    %br/
    .actions
      = f.submit 'Cadastrar', class: 'submit'