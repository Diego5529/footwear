#content
  #menu_left
    %h2
      %li Empresas
    - for enterprise in @enterprises
      %li
        = link_to enterprise.name, "/enterprise/#{enterprise.id}"
  #content_shoe
    %h1 Produtos da #{@enterprise.name}
    #mini_shoes
      = render :partial=>'mini_shoe', :collection=>@shoes
  #end