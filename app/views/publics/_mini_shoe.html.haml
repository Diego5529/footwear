#shoes_rand
  #shoes
    %b= mini_shoe.name
  %h2
    = link_to mini_shoe.enterprise.name, "/enterprise/#{mini_shoe.enterprise.id}"
    = image_tag(mini_shoe.image,height: 150, :class => "img")
    = simple_format(mini_shoe.category.name)
    %span.shoe_value= number_to_currency(mini_shoe.value)
    %br/
    =link_to (image_tag("bt_comprar.gif")),"/shoe/#{mini_shoe.id}"
    %br/
    %br/