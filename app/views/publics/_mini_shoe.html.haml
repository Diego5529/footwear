%h3 
  = mini_shoe.name
%h2
  = link_to mini_shoe.enterprise.name, "/enterprise/#{mini_shoe.enterprise.id}"
  %br/
  = image_tag(mini_shoe.image)
  = simple_format(mini_shoe.description)
  %span.shoe_value= number_to_currency(mini_shoe.value)
  %br/
  = link_to "More details","/shoe/#{mini_shoe.id}",:class=>"more_shoe_info"
  %br/
  %hr/	