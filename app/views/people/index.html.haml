.container
  #name_page
    Lista de Administradores
  %br/
  %table.tables
    %thead
      %tr
        %th Nome
        %th Senha
        %th Administrador
        %th Detalhes?
        %th Editar?
        %th Apagar?
    %tbody
      - @people.each do |person|
      - presenter = PersonPresenter.new(person)
        %tr
          %td= person.name
          %td= presenter.password
          %td= presenter.admin
          %td= link_to 'Detalhes', person
          %td= link_to 'Editar', edit_person_path(person)
          %td= link_to 'Apagar', person, method: :delete, data: { confirm: 'Tem certeza?' }
  %br/
  #form
    %h2= link_to 'Novo Administrador', new_person_path