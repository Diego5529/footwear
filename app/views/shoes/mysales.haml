.container
  #name_page
    Todos os pedidos
  %table.tables
    %thead
      %tr
        %th Código do pedido
        %th Data
        %th Código do produto
        %th Nome do produto
        %th Empresa
        %th Cliente
        %th Opções
    %tbody
      - @orders.each do |orders|
        %tr
          %td= orders.id
          %td
            = orders.created_at.day
            = orders.created_at
            \/
            = orders.created_at.month
            \/
            = orders.created_at.year
          %td
            = orders.shoe_id
          %td
            = orders.shoe.name
          %td
            = orders.shoe.enterprise.name
          %td
            = orders.client.name
          %td
            = link_to "Detalhes", "/mysalesdetails/#{orders.id}"